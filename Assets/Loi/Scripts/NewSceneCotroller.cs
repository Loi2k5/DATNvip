using UnityEngine;
using TMPro;
using UnityEngine.SceneManagement;

public class NewSceneCotroller : MonoBehaviour
{
    /*[SerializeField] private TextMeshProUGUI pointText;

    void Start()
    {
        int savedPoints = GameManager.Instance.currentPoints;
        pointText.text = $" {savedPoints}";
    }
    public void MuaPet(string petName)
    {
        if (GameManager.Instance.CurrentPoints >= 1)
        {
            GameManager.Instance.AddPoint(-1); // Trừ 1 điểm
            GameManager.Instance.SelectPet(petName);

            // Load màn chơi kế tiếp
            SceneManager.LoadScene("GameplayScene");
        }
        else
        {
            Debug.Log("Không đủ điểm để mua pet!");
        }
    }*/

}
